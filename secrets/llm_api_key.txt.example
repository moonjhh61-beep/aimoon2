# Optional fallback. HeadAgency prefers the HEAD_AGENCY_API_KEY environment variable.
# If the environment variable is unset, the loader reads the first non-empty, non-comment line below.
sk-your-api-key-goes-here
